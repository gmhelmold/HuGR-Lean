# HL-WP3.8 — Filesystem/Search Adversarial Review 01\n\n**Issue:** #36 — filesystem/search profiles  \n**Date:** 2026-09-26  \n**Disposition:** PASS after findings incorporated\n\n## Review objective\n\nChallenge whether search/listing compaction can remove bytes without silently converting exact evidence into a semantic summary.\n\n## Findings\n\n| ID | Severity | Finding | Resolution |\n|---|---|---|---|\n| P38-F01 | High | Generic ls/tree/find compaction would remove or reinterpret inventory data the user explicitly requested. | No filesystem listing profile admitted; ls/tree/find remain exact passthrough. |\n| P38-F02 | High | GNU/BSD grep dialects and context/binary/field variants are too broad for one safe generic parser. | grep deferred; no grep profile or backend rerun. |\n| P38-F03 | High | Grouping search results could reorder interleaved files. | rg parser admits only contiguous file groups and rejects path reappearance after another group. |\n| P38-F04 | High | Repeated search matches could be mistaken for duplicate noise. | Every match suffix is a mandatory verbatim Signal; regression proves identical match lines remain duplicated. |\n| P38-F05 | High | Partial parsing could reduce known-looking lines while dropping an unknown status/context line. | Every non-empty line must match the admitted grammar; one unknown line makes shape guard NoMatch. |\n| P38-F06 | Medium | Colon-delimited grammar is ambiguous for Windows/colon-containing paths. | Paths containing colon are rejected; such output remains passthrough. |\n| P38-F07 | Medium | Context/heading/JSON/column/null/count modes change output grammar. | Known format-changing flags are excluded at identity admission and donor fixtures lock passthrough behavior. |\n| P38-F08 | Medium | Search grouping could become a relevance or truncation feature. | Profile only removes repeated exact path prefixes; no match truncation, ranking, dedupe, or semantic filtering exists. |\n| P38-F09 | Medium | Search result order may be evidence. | Group order and match order are preserved exactly; only repeated path text moves to one group header. |\n| P38-F10 | Medium | Standalone CR could be mistaken for line presentation. | Any CR in admitted rg baseline rejects the profile; no CR normalization occurs here. |\n| P38-F11 | Medium | Donor parser/truncation behavior could leak in with copied fixtures. | Only fixture bytes are copied from pinned TRS; HuGR parser is newly authored TypeScript and truncation behavior is rejected. |\n\n## Supported surface\n\n- direct `rg` identity only;\n- complete + exited observations;\n- exit code 0 for match-shaped output;\n- native non-heading `path:line:content` shape;\n- contiguous grouping only;\n- at least one repeated path so reduction is materially plausible;\n- exact path and every match suffix preserved as Signals.\n\n## Deferred/rejected surface\n\n- grep — deferred/passthrough;\n- ls/tree/find — deferred/passthrough;\n- read/source content — exact passthrough;\n- heading/context/JSON/vimgrep/column/null/count/files-only/replace/only-match/color modes — passthrough;\n- ambiguous colon paths — passthrough;\n- binary notices — passthrough;\n- interleaved paths — passthrough.\n\n## Final disposition\n\n**PASS**\n\nThe admitted reducer is presentation grouping only. It does not change search semantics or decide which matches matter.\n